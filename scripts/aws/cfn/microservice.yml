AWSTemplateFormatVersion: 2010-09-09
Description: 'pn-io-connector-mock microservice'

Parameters:
  ProjectName:
    Type: String
    Description: 'Usually pn can be pnXYZ where XYZ are the feature number, useful to create
        experimental environments without crash official development environment'

  AlarmSNSTopicArn:
    Type: String
    Description: ARN of alarm topic

  ContainerImageUri:
    Type: String
    Description: 'Exact container image URI with full repository and image digest'

  MicroserviceNumber:
    Type: Number
    Description: 'Disambiguation useful for load balancer rules'

  TemplateBucketBaseUrl:
    Type: String
    Description: 'The S3 bucket from which to fetch the templates used by this stack.'

  ECSClusterName:
    Type: String
    Description: 'The name of the ECS cluster where the microservice is going to be deployed'

  VpcEgressSubnetsIds:
    Type: String
    Description: 'Subnets where the microservice egress traffic is routed'

  VpcId:
    Type: String
    Description: 'VpcId where the microservice is going to be deployed'

  EcsDefaultSecurityGroup:
    Type: String
    Description: 'Default security group required by infrastructure'

  ApplicationLoadBalancerListenerArn:
    Type: String
    Description: 'Internal load balancer listener where the HTTP endpoint is registered'

  ApplicationLoadBalancerDomain:
    Type: String
    Description: 'Base URL of the internal load balancer where the service is reachable'

  AlbSecurityGroup:
    Type: String
    Description: 'Application load balancer security group'

  LogsKinesisSourceStreamArn:
    Type: String
    Description: 'Kinesis stream that receives logs'

  Version:
    Type: String
    Description: 'keep track of used projects commitIds'

  EcsLogGroup:
    Type: String
    Description: 'CloudWatch log group name (from storage.yml output)'

  HealthCheckInterval:
    Type: Number
    Default: 60

  HealthCheckTimeout:
    Type: Number
    Default: 5

  HealthyThresholdCount:
    Type: Number
    Default: 5

  UnhealthyThresholdCount:
    Type: Number
    Default: 2

  CpuValue:
    Type: Number
    Default: 1024
    Description: Fargate virtual CPU quantity, 1024 equals one vCPU

  MemoryAmount:
    Type: String
    Default: 2GB
    AllowedValues: [ 2GB, 4GB, 6GB, 8GB ]

  AutoscalingCheckPeriod:
    Default: 60
    Type: Number

  AutoscalingDataPointN:
    Default: 1
    Type: Number

  AutoscalingDataPointM:
    Default: 1
    Type: Number

  AutoscalingThreshold:
    Default: 70
    Type: String

  MinTasksNumber:
    Default: 1
    Type: Number

  MaxTasksNumber:
    Default: 1
    Type: Number

  LogAlarmStrategy:
    Type: String
    Default: 'FATAL'

  WireTapLogActivation:
    Type: String
    Default: 'false'
    AllowedValues: [ 'true', 'false' ]

  PnCronAnalyzer:
    Type: String
    Default: '-'

  FargateEFSFileSystemID:
    Type: String
    Description: "EFS Filesystem"

  MountEfs:
    Type: String
    Default: "false"
    AllowedValues:
      - "true"
      - "false"

  LogsKinesisFilterPattern:
    Type: String
    Default: ''

  ApplicativeEnvFileChecksum:
    Type: String
    Default: ''

  # -- Mock specific SSM parameter names ---------------------------------------------
  MockSequenceParameterNames:
    Type: String
    Default: 'MapIoConnectorMockSequence'
    Description: 'Comma separated list of SSM parameter names holding the sequences.'

  SenderNotAllowedParameterName:
    Type: String
    Default: 'MapIoConnectorMockSenderNotAllowed'
    Description: 'SSM parameter name holding the deny-list.'

  PnIoConnectorMockManagedPolicyName:
    Type: String
    Default: 'PnIoConnectorMockManagedPolicyName'
    Description: 'Name of the managed policy associated to the mock task role.'

Resources:
  PnIoConnectorMockMicroservice:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub '${TemplateBucketBaseUrl}/fragments/ecs-service.yaml'
      Parameters:
        MicroServiceUniqueName: !Sub '${ProjectName}-io-connector-mock'
        CpuValue: !Ref CpuValue
        CpuArchitecture: ARM
        MemoryAmount: !Ref MemoryAmount
        HealthCheckPath: '/api/v1/actuator/health'
        HealthCheckTimeout: !Ref HealthCheckTimeout
        HealthCheckInterval: !Ref HealthCheckInterval
        HealthyThresholdCount: !Ref HealthyThresholdCount
        UnhealthyThresholdCount: !Ref UnhealthyThresholdCount
        AutoscalingStrategy: REST-API
        AutoscalingThreshold: !Ref AutoscalingThreshold
        AutoscalingCheckPeriod: !Ref AutoscalingCheckPeriod
        MinTasksNumber: !Ref MinTasksNumber
        MaxTasksNumber: !Ref MaxTasksNumber
        AutoscalingDataPointN: !Ref AutoscalingDataPointN
        AutoscalingDataPointM: !Ref AutoscalingDataPointM
        AlarmSNSTopicArn: !Ref AlarmSNSTopicArn
        EfsFilesystem: !Ref FargateEFSFileSystemID
        MountEfs: !Ref MountEfs
        ContainerImageURI: !Sub '${ContainerImageUri}'
        ContainerEnvEntry1: !Sub 'AWS_REGIONCODE=${AWS::Region}'
        ContainerEnvEntry2: !Sub 'WIRE_TAP_LOG_ACTIVATION=${WireTapLogActivation}'
        ContainerEnvEntry3: !Sub 'PN_CRON_ANALYZER=${PnCronAnalyzer}'
        ContainerEnvEntry4: !Sub 'PN_IOCONNECTORMOCK_MOCKSEQUENCEPARAMETERNAMES=${MockSequenceParameterNames}'
        ContainerEnvEntry5: !Sub 'PN_IOCONNECTORMOCK_SENDERNOTALLOWED_PARAMETERNAME=${SenderNotAllowedParameterName}'
        MappedPaths: '/io-connector-mock/message*,/io-connector-mock/profile*'
        ECSClusterName: !Ref ECSClusterName
        Subnets: !Ref VpcEgressSubnetsIds
        VpcId: !Ref VpcId
        EcsDefaultSecurityGroup: !Ref EcsDefaultSecurityGroup
        LoadBalancerListenerArn: !Ref ApplicationLoadBalancerListenerArn
        LoadbalancerRulePriority: !Ref MicroserviceNumber
        AlbSecurityGroup: !Ref AlbSecurityGroup
        TaskRoleManagedPolicyArn: !Ref PnIoConnectorMockManagedPolicy
        EcsLogGroup: !Ref EcsLogGroup
        LogAlarmStrategyV1: !Ref LogAlarmStrategy
        ApplicativeEnvFileChecksum: !Ref ApplicativeEnvFileChecksum

  PnIoConnectorMockManagedPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: 'Managed policy granting pn-io-connector-mock read access to its SSM parameters only'
      ManagedPolicyName: !Ref PnIoConnectorMockManagedPolicyName
      PolicyDocument:
        Version: 2012-10-17
        Statement:
          - Effect: Allow
            Action:
              - ssm:GetParameter
              - ssm:GetParameters
            Resource: !Sub 'arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/MapIoConnectorMock*'
